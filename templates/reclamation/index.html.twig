{% extends 'base.html.twig' %}

{% block title %}Mes Reclamations{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@tabler/icons-webfont@latest/tabler-icons.min.css">
    <link rel="stylesheet" href="{{ asset('css/etudiants.css') }}">
{% endblock %}

{% block body %}
<div class="etu-page">
    <div class="etu-header">
        <h1 class="etu-header-title">Reclamations</h1>
    </div>

    <div class="etu-table-wrap">
        <table class="etu-table">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Date Creation</th>
                    <th>Statut</th>
                    <th>Controle</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
            {% for reclamation in reclamations %}
                <tr>
                    <td>{{ reclamation.id }}</td>
                    <td>{{ reclamation.dateCreation ? reclamation.dateCreation|date('d/m/Y') : '' }}</td>
                    <td>
                        <span style="font-weight: bold; color: {% if reclamation.statut.value == 'EN_ATTENTE' %}#f59e0b{% elseif reclamation.statut.value == 'ACCEPTEE_PAR_LE_PROFESSEUR' or reclamation.statut.value == 'ACCEPTEE_PAR_ADMINISTRATEUR' %}#10b981{% else %}#ef4444{% endif %};">
                            {{ reclamation.statut.value|default(reclamation.statut) }}
                        </span>
                    </td>
                    <td>{{ reclamation.controleId }}</td>
                    <td class="etu-td-actions">
                        <a href="{{ path('app_reclamation_show', {'id': reclamation.id}) }}" class="etu-btn-icon" title="Voir"><i class="ti ti-eye"></i></a>
                        <a href="{{ path('app_reclamation_edit', {'id': reclamation.id}) }}" class="etu-btn-icon" title="Modifier"><i class="ti ti-pencil"></i></a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="5" style="text-align: center; padding: 20px;">Aucune réclamation trouvée</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
</div>
{% endblock %}
